// import React from "react";
// import { useSelector } from "react-redux";

// export const patternsFilter = ({ name: value }) => {
//   const patterns = useSelector((state) => state.patterns);
//   console.log("state patterns", patterns);
//   const pattern = {};
//   const pattern = patterns.filter((pattern) => {
//     pattern[name] === value;
//   });
//   return pattern;
// };
